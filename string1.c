#include<stdio.h>
#include<string.h>
int main()
{
   char s[]="Hello";
    // int p;
    // char s[] ="Marvel";
    // printf("%s",&s);
    // p=strlen(s);
    // printf("\nLength of a String %d ",p);
    if(s == ("Hello") || ("Good") || ("Bad")){
    printf("Ok");
    }
    else
    {
        printf("NICE");
    }
    

}