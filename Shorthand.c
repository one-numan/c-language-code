#include <stdio.h>
int main()

{ 
    int a = 8;
    (a == 8) ? (printf("a is 8")) : (printf("8 is not a"));
    //Its both for if and else
    (a == 8) && (printf("\n\nOKayokay")); //only for IF part

    (a ==5 ) || (printf("\n\n Him How")); //only for ELSE part
// int number=0,integer =2;
}

