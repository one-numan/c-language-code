#include <stdio.h>
void main()
{
    int a, n;
    printf("enter any number "))&&(scanf("%d",&a));
    while (n>0)
    {
        n = a % 10;
        int sum += n ;
        int pow *= n ;
    }
    if (sum==pow)
    {
        printf("its a Spy");
    }
    else{
        printf("its not a spy");
        }

}